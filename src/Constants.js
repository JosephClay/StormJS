//###################################################################################
// Constants ########################################################################
//###################################################################################

var STORM = {};
_extend(STORM, {
	version: '0.0.1',
	name: 'Storm',
	category: {
		nonblocking: 0,
		blocking: 1
	}
});

Storm.constants = function() { return STORM; };